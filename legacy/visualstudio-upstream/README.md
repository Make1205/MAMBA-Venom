# Upstream Visual Studio project files

This directory contains upstream Visual Studio project files retained for provenance only.
These files are not maintained MAMBA-Frost build files.
They are not used by the default build, Frost KAT generation, benchmarks, or paper data.
The supported current build path is the Unix Makefile flow under `Frost/`.
